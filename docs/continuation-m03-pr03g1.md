# M03-PR03g1 executor-foundation continuation

M03-PR03g1 establishes a private 173-descriptor/173-source-site disposable V2
executor foundation in the existing `och-v2-evidence` tooling package. The sole
store-child I/O owner executes every retained source site under success and every
g1-applicable error/partial/pressure mode, plus legal compact P0-P7 present and
absent cleanup, rollback, and 64-pair eager-open flows. Safe-Rust SHA-256,
primitive relationship, fingerprint, entry, inventory, and refusal bounds remain
private test/tooling proof. Cycle 1 hardens that foundation with always-attempted
named-child disposal, immediate pre-restore short-write state evidence, and a
closed alias-resistant production-module/source-I/O ownership policy.

The exact tooling-only runtime/Tokio authority is unchanged: runtime activates
only `m03-pr03e-native-harness`, Tokio remains pinned at `1.53.1` with only `rt`
and `sync`, defaults remain native-only, the native closure remains five packages,
and no `sha2` package or native-to-tooling edge exists.

This delivery has no complete matrix result, report bundle, collector, child
supervisor, measurement, collection authorization, accepted V2 artifact, or
product behavior. `CHILD_CRASH_AFTER_SUCCESS` remains a declared g2 obligation.
Every `PR03E-M01..M11` row remains `UNSATISFIED`, and all native limits remain
`UNKNOWN`.

The next permissible slice is M03-PR03g2 complete harness/collection
infrastructure. Only its separate acceptance may authorize Linux collection;
measured evidence and a fresh owner checkpoint still precede product planning.
